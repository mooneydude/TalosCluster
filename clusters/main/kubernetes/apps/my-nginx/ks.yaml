apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: my-nginx
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/apps/my-nginx/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

